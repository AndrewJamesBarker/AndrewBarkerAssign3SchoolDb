
@{
    ViewBag.Title = "New";
}

@*<head >
        <script src="~/Scripts/script.js"></script>
    </head>*@

<div id="containershow">
    <nav>
        <a class="navlink" href="/Teacher/List">Back to Teachers</a>
    </nav>
    <h2>New Teacher</h2>
    <form onsubmit="formValidate()" method="POST" action="/Teacher/Create" name="formTeacher">
        <div>
            <label for="TeacherFname">First Name: </label>
            <input type="text" id="TeacherFname" name="TeacherFname" />
            <label id="fNameLabel" for="TeacherFname"></label>
        </div>
        <div>
            <label for="TeacherLname">Last Name: </label>
            <input type="text" id="TeacherLname" name="TeacherLname" />
            <label id="lNameLabel" for="TeacherLname"></label>
        </div>
        <div>
            <label for="EmployeeNumber">Employee Number: </label>
            <input type="text" id="EmployeeNumber" name="EmployeeNumber" />
            <label id="eNumberLabel" for="EmployeeNumber"></label>
        </div>
        <div>
            <label for="Salary">Salary: </label>
            <input type="text" id="Salary" name="Salary" />
            <label id="salaryLabel" for="Salary"></label>
        </div>
        <div>
            <label for="HireDate">Hire Date: </label>
            <input type="datetime" id="HireDate" name="HireDate" placeholder="yyyy-mm-dd" />
            <label id="hireDateLabel" for="HireDate"></label>
        </div>
        <div>
            <input type="submit" value="Create Teacher" />
        </div>
    </form>

    <script>

        window.onload = function () {

            /*alert("it's working!");*/

            var formHandle = document.forms.formTeacher;

            formHandle.onsubmit = formValidate;

            function formValidate() {

                var fNameLabel = document.getElementById("fNameLabel")
                var lNameLabel = document.getElementById("lNameLabel")
                var eNumberLabel = document.getElementById("eNumberLabel")
                var salaryLabel = document.getElementById("salaryLabel")
                var hireDateLabel = document.getElementById("hireDateLabel")

                var fNameValue = formHandle.TeacherFname;
                var lNameValue = formHandle.TeacherLname;
                var employNumValue = formHandle.EmployeeNumber;
                var salaryValue = formHandle.Salary;
                var hireDateValue = formHandle.HireDate;
                var hireDateRegex = /^([12]\d{3}-(0[1-9]|1[0-2])-(0[1-9]|[12]\d|3[01]))$/;


                if (fNameValue.value === "" && lNameValue.value === "" && employNumValue.value === "" && salaryValue.value === "" && hireDateValue.value === "") {
                    alert("enter all fields!");
                    return false;
                }

                if (fNameValue.value === "") {
                    fNameValue.style["background-color"] = "red";
                    fNameValue.focus();
                    fNameLabel.innerHTML = "Please enter the first name."
                    return false;
                }
                if (lNameValue.value === "") {
                    lNameValue.style["background-color"] = "red";
                    lNameValue.focus();
                    lNameLabel.innerHTML = "Please enter the last name."
                    return false;
                }
                if (employNumValue.value === "") {
                    employNumValue.style["background-color"] = "red";
                    employNumValue.focus();
                    eNumberLabel.innerHTML = "Please enter the employee number."
                    return false;
                }
                if (salaryValue.value === "") {
                    salaryValue.style["background-color"] = "red";
                    salaryValue.focus();
                    salaryLabel.innerHTML = "Please enter the salary."
                    return false;
                }
                if (hireDateValue.value === "") {
                    hireDateValue.style["background-color"] = "red";
                    hireDateValue.focus();
                    hireDateLabel.innerHTML = "Please enter the hire date."
                    return false;
                }
                if (!hireDateRegex.test(hireDateValue.value)) {
                    alert("Please enter date in YYYY-MM-DD format.");
                    hireDateValue.style["background-color"] = "red";
                    hireDateValue.focus();
                    return false;
                }
                return true;
            }
        }
    </script>

</div>

             






